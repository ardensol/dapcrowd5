<p id="notice"><%= notice %></p>

<div id="campaign">
  <div id="funding_area">
    <div class="container clearfix">
      <div class="center">
        <h2 style="font-size: 30px"><%= @lab.name %></a></h2>
      </div>
    <% if @lab.status == "voting" %>
      <%= render 'votingheader'%>
    <% elsif @lab.status == "developing" %> 
      <%= render 'developingheader' %>
    <% elsif @lab.status == "crowdfunding" %>
      <%= render 'crowdfundingheader' %>
    <% end %>    
  </div>
</div>
</br>
<div id="campaign_body">
  <div class="container clearfix">
    <% if @lab.status == "voting" %>
      <%= render 'votingmain' %>
    <% elsif @lab.status == "developing" %>
      <%= render 'developingmain' %>
    <% elsif @lab.status == "crowdfunding" %>
      <%= render 'crowdfundingmain' %>
    <% end %>
  </div>
</div> 
<script>

$(window).load(function(){ 

  $(function(){

    $('#masonry-container').masonry({
      itemSelector: '.box',
      columnWidth: 100,
      gutterWidth: 10,
      isFitWidth: true
    });
  });
});

</script>

 <script>

    $(document)

      .on('ajax:send', '.description', function () { $(this).addClass('loading'); })
      .on('ajax:complete', '.description', function () { $(this).removeClass('loading'); })
      .on('ajax:error', '.description',function () { $(this).after('<div class="error">There was an issue.</div>'); })
      .on('ajax:success', '.description', function(e, data, status, xhr) { $(this).find("span").html(data.count); });

  </script>
  <script src="js/bootstrap.min.js"></script>