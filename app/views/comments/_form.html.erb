<%= form_for [@commentable, @comment] do |f| %>
	<%= f.text_area :content, rows: 6, style: "width: 500px;" %>
	<%= f.submit "Submit", class: "button hero-button", style: "margin-top: 80px;" %>
<% end %>